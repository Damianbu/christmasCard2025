If git push is not working:

Go to Github Settings -> Developer Settings -> Create personal Access Token classic. Save for later !

Open the Console, Konsole, Terminal or Shell. 

Navigate to the local Repo Folder. Post the Command "git remote set-url origin https://[TOKEN]@github.com/[USERNAME]/[REPOSITORY] 

Press Enter and then the git push will work.
